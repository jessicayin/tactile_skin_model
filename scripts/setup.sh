#!/bin/bash

# Set the PYTHONPATH to include the current directory (or specify the path to your codebase)
export PYTHONPATH=$PYTHONPATH:$(pwd)

# Optionally, print the PYTHONPATH to verify
echo "PYTHONPATH set to: $PYTHONPATH"